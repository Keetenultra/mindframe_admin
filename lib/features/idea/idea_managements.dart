import 'package:flutter/material.dart';

class IdeaManagements extends StatelessWidget {
  const IdeaManagements({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
